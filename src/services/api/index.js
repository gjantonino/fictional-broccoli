import { addActivity } from './addActivity';
import { getActivityByUser } from './getActivityByUser';
import { getLocations } from './getLocations';

export const fieldActivityRest = {
    addActivity,
    getActivityByUser,
    getLocations
}